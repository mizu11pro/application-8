<h1>BOOKS</h1>
<% @books.each do |book| %>

<p>title</p>
<span><%= book.title %></span>
<p>body</p>
<span><%= book.body %></span>
<tr>
  <td><%= link_to 'show', booklist_path(book.id) %></td>
</tr>

<% end %>